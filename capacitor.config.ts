import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.laesquina.myapp',
  appName: 'La_Esquina',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
